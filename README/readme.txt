.LOG

version1预期功能：
编写基础的nrf接收发送模块，可以利用nrf进行数据通讯，各组件可以模块化
四路LED控制模块，可以简洁快速的对LED进行点亮，关闭，流水，闪烁操作
四路按键控制模块，可以对按键进行扫描操作，调用函数时可以返回该函数所对应的值
四路拨码开关模块，可以通过键盘扫描确定该模块的值，并返回对应值

13:54 2018/11/10 星期六

编写蜂鸣器模块，输入不同的值可以发出不同的声音，不同声音之间会有音调的变化
测试各个模块，除去nrf因为板子问题而没有测试

模式选择定义：
mode0：参数调试模式，按键调整不同的参数，OLED显示
mode1：工作模式，按键为中断控制方式，功能通过OLED显示，用于向核心板发送一些固定的代码等等
